# Nova Path — Frontend (React + Vite)

This folder is the web client for Nova Path.

**Full project overview, Azure URLs, test credentials, grader notes, ML/security bullets, and local run steps** are documented in the [**repository root README**](../README.md).

Quick start: from this directory run `npm install` and `npm run dev`.
